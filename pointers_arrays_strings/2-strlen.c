#include "main.h"
#include <stdio.h>

/**
 * _strlen - swap les pointer
 *@s: pointos
 * Return: Always 0.
 */

int _strlen(char *s)

{
	int longueur;

	for (longueur = 0; s[longueur] != '\0'; longueur++)
	{

	}
	return (longueur);
}
