#include "main.h"

/**
 * print_alphabet - Desc de la fonction
 * Return: return rs
*/

void print_alphabet_x10(void)
{
int loop10;
for (loop10 = 0; loop10 < 10; loop10++) {
		int i;
	for (i = 'a'; i <= 'z'; i++)
{
	_putchar(i);
}
_putchar('\n');
}
}
